<ul class="mt"> 
    {%for link in links[page.lang] %}
    <li class="unstyle-li">{{link.emoji}} 
        <a href="{{link.link}}" target="_blank" rel="noopener noreferrer" class="unstyle-a animation">{{link.desc}} <span class="visually-hidden">{{ languages[page.lang].newTab }}</span></a>
    </li>
    {% endfor %}
</ul>